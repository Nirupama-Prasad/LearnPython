import random
import sys
import os

# tuples are like constant lists ie cant be changed later

pi_tuple = (3,1,4,5,9)

#type conversion
new_tuple = list(pi_tuple)
new_list = tuple(new_tuple)

print new_tuple
print new_list

#fns to use with tuples
len(pi_tuple)
max(pi_tuple)
min(pi_tuple)


